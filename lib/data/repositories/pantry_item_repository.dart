import 'package:pantry_app/data/database/db_provider.dart';

class PantryItemRepository {
  final dbProvider = DBProvider();

  // TODO: handle pantry entries
  // (barcode, quantity, expirationDate)
}
